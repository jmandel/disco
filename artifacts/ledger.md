# Variability ledger — gauntlet @ localhost:4820 (session `dryrun`, 2026-08-30)

What varied, what is suspected to vary, and what would resolve it (GUIDANCE §7.5). Evidence = act ids
and rows in `sessions/dryrun/store.sqlite`; re-inspect with `disco sql` / `openStore` rather than re-trusting.
Suspicions marked *prior* come from the enterprise-SPA prior (§2.4/§8), not observation.

| # | step | observed (n) | varied / surprise | hypothesis | resolving experiment |
|---|---|---|---|---|---|
| 1 | record open → "Allergy Review Required" modal | 5/5 successful opens produced the modal: acts 4,7,8,12,48; sentinel dialog seq 1-5 | modal appears **~418-446ms after the record fetch settles** (fetch settles 60-129ms; sentinel deltas +418/+431/+430/+446ms), i.e. *after* the action's own report | delay = ctl `modalDelayMs:400` (header leaks config); gate = ctl `modal:true`; content suggests condition = `record.allergies` non-empty — all 5 sampled records have allergies, so the conditional branch is **unproven** (§2.4: honest claim is "n=5/5, condition unknown") | (a) `POST /ctl {"modal":false}`, reopen a record, expect no modal; (b) find/arrange an allergy-free record; (c) vary `modalDelayMs` and re-measure sentinel delta |
| 2 | click while modal up | 2 distinct failure shapes: act:5 (click "succeeded", verdict settled:visual, **zero wire, record pane unchanged** — silently swallowed) vs act:10 (verdict `diagnosis`, reason `occluded`, occludedBy `#record-modal`) | which shape you get depends on whether the modal is up at hit-test time vs appears mid-flight | swallow happens when the overlay appears between resolution and dispatch | scripts must verify postcondition on the wire (attributed `GET /api/record/<n>`), never trust a non-error verdict; `scripts/open-record.ts` does this |
| 3 | save | n=2 (acts 14, 50), identical: screen "Saved ✓" at ~34-40ms, wire `POST /api/save → 202 {"pending":true}`, then `GET /api/save/status → 200` at ~+520ms (attribution `none` — post-window), toast "Saved" (sentinel toast seq 5) | none across n=2 | failure branch exists but unobserved: ctl `saveFails:false` | `POST /ctl {"saveFails":true}` then save: expect screen still optimistic, toast/status carrying failure — the §8 "optimistic UI" trap in full |
| 4 | push-channel delivery (§23) | ws n=1 (ws_frames `{"type":"notify","via":"ws"}`, t≈421689), sse n=1 (sse_events, request `/api/notify-sse`), poll n=1 (`{"n":3,"via":"poll","text":"Result 3 via poll"}` in the held `/api/notify-poll` body, t_start 402032) | poll delivery latency: the notification rides the **held long-poll** (hold ≈25s, ctl `notifyPollHoldMs`), so screen update lagged my check — UI showed count 2 when the wire later proved delivery | all three channels deliver; poll is release-on-push or release-on-hold-expiry (indistinguishable at n=1) | push poll again and watch the in-flight notify-poll: does it return immediately (release-on-push) or at hold expiry? |
| 5 | login / secure area | n=1: guessed creds → `POST /api/login → 401` (act:45); `/secure.html` → 302-style redirect to `/login.html?next=/secure.html` (act:46) despite ctl `requireAuth:false` | 401 body **unread** (body_state) — any hint in it is lost | valid creds unknown; `requireAuth` likely gates something else (API?) since secure.html redirected anyway | ask the user for demo creds; toggle `POST /ctl {"requireAuth":true}` and diff which endpoints start 401ing |
| 6 | session timeout | never observed; §12 shows "idle timer: off", ctl `timeoutMs:0` | — | warning modal exists behind ctl `timeoutMs` | `POST /ctl {"timeoutMs":5000}`, idle, let the session-expiry sentinel catch the warning |
| 7 | hover over #rerender | n=1 (act:23): verdict `no-effect` **but** `data-gen` advanced 1877→1889 during the window (ctl `rerenderOnHover:true`) | screen changed while the report said nothing happened | mutations landed but were batched/classified out, or arrived after the ~500ms no-effect tier | repeat hover with `noEffectMs`/budget raised; check mutations table rows in the window; tool-side question (see friction.md #6) |
| 8 | header status line | mutates on *every* WS frame (echo per click, push every ~7s) and shows live `effective ctl` JSON | constant DOM churn adjacent to every action | header is both a confound (DOM quiescence noise) and a gift (config + last-frame visible on screen) | none needed; noted for settlement interpretation |
| 9 | ambient profile | heartbeat 5000ms gaps cv=0 (n=129); poll 3004ms cv=0 (n=218); notify-poll 25004ms cv=0.14 (n=27) — all auto-classified ambient `periodic` | §5 text says ambient "default off" but ctl shows `ambient:true` in this environment | env-specific toggle | none; record per-environment |
| 10 | first-run / return-visit | 3 full page loads (initial, act:3 reload, act:26/47 returns) — no first-run tips or "what's new" seen (n=3) | — | *prior*: such interstitials are common; none in this app so far | watch on a fresh browser profile (cookies/localStorage clean) |
| 11 | debounced search | n=1 (act:15): typing "ada" → exactly one trailing `GET /api/search?q=ada` (attribution `window`), settled:dom 632ms | — | 250ms trailing debounce per §7 heading; per-keystroke requests might appear with slower typing | type with >250ms inter-key gaps; expect one request per pause |
| 12 | GraphQL write classification | query act:20 plain; mutation act:21 flagged ✎write per-request; family `POST /api/graphql` stays `write_kind:read` | same family, mixed semantics | body-peek (`mutation` keyword) drives the per-request flag | none; automation should trust per-request flags over family for GraphQL |
| 13 | canvas grid | act:28 click: 0 requests, visuals 1, DOM mutations only from the header echo; grid *content* is wire-available (`GET /api/grid` → rows/cols/cells JSON) | no app-level reaction discovered for clicks | cells may react to precise coordinates (pixels-only feedback) | click cell centers computed from `/api/grid` geometry and diff screenshots |
| 14 | child window | n=1 (acts 40-41): auto-attached, instrumented, `/api/child-ping` captured, detach recorded | — | *prior*: child windows may vary by feature (print/sign) | none for this app |
| 15 | drag settlement | slider act:36 1528ms, reorder act:37 1447ms — both settled:network on trailing `POST /api/drag-report`; ambient heartbeat+poll correctly tagged `ambient` inside act:37's window | drags settle ~10× slower than clicks | drag-report fires on drop with a delay | acceptable; budget drags ≥2.5s |

## Standing open questions for the user
1. Valid login credentials for `/login.html` (unlocks the `?next=` redirect flow and the secure area).
2. Is mutating `/ctl` in scope for a future session? It gates every major conditional branch (modal, saveFails, timeoutMs, requireAuth, ambient) and would turn ledger rows 1, 3, 5, 6 from hypotheses into observations cheaply.
