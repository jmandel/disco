# apps/

One folder per app you drive. What goes in it — and why — is in the root README under **The app folder**.
`apps/<app>/store/` is the log (gitignored); everything else is documentation and code you keep.
