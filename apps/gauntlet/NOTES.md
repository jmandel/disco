# gauntlet — notes

Appended by `disco note` / `s.note()`. Distill into README.md when it settles.

- [run 1 · 603008ms] SPA served from :4800; single page /, all state via JSON fetch/XHR + WS + SSE + long-poll. Server knobs at POST /ctl (see /ctl for current state); POST /ctl/reset restores defaults.
- [run 1 · 603026ms] Load Chart: GET /api/slow?ms=<slowMs> races GET /api/chart/a & /api/chart/b (Promise.all). Anchor until: {selector:'#chart:has-text("Chart loaded")'} or {request:'/api/slow',landed:true}. status span goes loading… then idle. (act:2)
- [run 1 · 603026ms] Records: click #record-N -> GET /api/record/N -> fills #record-fields. Conditional modal only when ctl.modal=true (delayed modalDelayMs). Handle both: until any of #record-modal or #record h3. #modal-ack closes it. Modal is role=dialog overlay that occludes record buttons. (act:39-42)
- [run 1 · 603026ms] Save: optimistic. Click #save sets 'Saved ✓' immediately; POST /api/save 202 {id,pending} then after ~500ms GET /api/save/status?id -> 200(ok)/500(fail, ctl.saveFails). Toast #toast[data-kind=ok|fail]. Body of /save/status is 'missing' (page reads only r.ok) - wait on {request:'/api/save/status'} and read status, or on the toast. (act:3, act:43-44)
- [run 1 · 603026ms] Spinner #s-4 spins forever; there is no request and it never goes gone. Do not wait on it. (act:9)
- [run 1 · 603026ms] WebSocket /ws opens on load: server sends {type:hello} then echoes every button click as {type:action,id} -> {type:echo,id,seq}. #noop is the only button that does NOT send. To capture frames, open the page inside your own session (navigate) since WS is created at load. (act:11)
- [run 1 · 603026ms] Debounced search #search: 250ms trailing XHR GET /api/search?q= . Use type OR fill (fill fires one input event, still triggers). until {request:'/api/search',landed:true}. (act:12-13)
- [run 1 · 603026ms] Virtualized rows: click #load-rows -> GET /api/rows (10000 rows, 485KB JSON). Only ~23 .row divs in DOM (windowed, ROW_H=24, VIEW_H=400). Truth is on the wire, not the DOM. Scroll #rows to page. (act:14)
- [run 1 · 603026ms] Re-render race #rerender: replaced every 100ms and on mousemove (ctl.rerenderOnHover). A real mouse click -> diagnosis detached. Fixes: js:true (dispatched click), or ctl {rerenderOnHover:false} then normal click. until on #rerender-count incrementing. (act:15-16, act:75)
- [run 1 · 603026ms] Iframes: #same-origin (/iframe.html) has nested #nested2 (/iframe2.html). #cross-origin loads xframe.html from :4801. Drive with frame:'#same-origin', nested frame:'#same-origin >> #nested2', cross frame:'#cross-origin'. Submits POST /api/iframe-submit / /api/xframe-submit. (act:30-35)
- [run 1 · 603026ms] Dialogs: #confirm->confirm(), #alert->alert(), #arm-unload arms beforeunload then #nav-away triggers it. disco default dialogs:'accept'. With accept, beforeunload lets nav proceed to /away.html. (act:37-38, act:77)
- [run 1 · 603027ms] Session timeout: ctl.timeoutMs>0 arms idle timer; fires #session-timeout dialog; #stay re-arms. Any mouse/key/click resets the timer. (act:61-62)
- [run 1 · 603027ms] Disabled #noop-disabled -> diagnosis disabled in ~100ms; it is wrapped in .field-wrap which is the hit-test parent. #noop is a true no-op (no wire, no WS). (act:4-5)
- [run 1 · 603027ms] Delete: click #delete -> DELETE /api/item/1 -> {deleted:1}. Write endpoint. (act:6)
- [run 1 · 603027ms] Child window: #open-child -> window.open('/child.html'). Appears in s.context.pages() / report 'new page'. Its #child-fetch -> GET /api/child-ping {pong:true}. (act:36, act:79)
- [run 1 · 603027ms] Canvas grid #grid: pixels only, no DOM per cell. Click selects a cell; app exposes window.__gridSelected={r,c}. until {fn:'window.__gridSelected'} then read it. (act:27)
- [run 1 · 603027ms] Keyboard combobox #med: type (keystrokes) -> GET /api/meds?q -> #med-list options. Mouse click on option is occluded/ignored (mousedown preventDefault). Recipe: type, until request meds landed, press ArrowDown (until li[aria-selected=true]), press Enter (until #med-selected has 'Selected'). (act:17-20)
- [run 1 · 603027ms] Shadow DOM: #shadow-host open shadow root, button reachable via chained selector '#shadow-host >> #shadow-btn'. (act:21)
- [run 1 · 603027ms] SSE #start-sse: EventSource /api/sse emits 5 events ~500ms apart then closes; #sse-status idle->connecting->open->done. Response body_state 'missing' (event-stream never handed over) but each event lands in #sse-log. (act:52)
- [run 1 · 603027ms] GraphQL over POST /api/graphql {query}. query{patient{name}} -> Ada Lovelace; mutation{rename} -> echoes name but does NOT persist (query after still Ada). sawMutation/operation flags in body. (act:7)
- [run 1 · 603027ms] Auth: /secure.html and (when ctl.requireAuth) / redirect 302 -> /login.html?next=. POST /api/login accepts ANY user/pass, sets cookie gauntlet_auth=<user> (HttpOnly), then location.href=next. Secure page shows 'Welcome, <user>'. (act:64-73)
- [run 1 · 603027ms] Push channels (s-23): standing #notif-count. POST /ctl {push:'ws'|'sse'|'poll'} injects one notification. ws & sse deliver immediately; poll only when ctl.notify=true (client runs notify-poll loop, held up to notifyPollHoldMs). (act:54-57)
- [run 1 · 603027ms] Context menu: rightclick #ctx-target reveals #ctx-menu (role=menu, was hidden). Click a menuitem -> #ctx-result. (act:22-23)
- [run 1 · 603027ms] Double-click edit #dbl-target: single click after 250ms -> 'selected'; dblclick -> input #dbl-input 'editing'; Enter -> 'committed: <val>'. (act:24-26)
- [run 1 · 603027ms] Drag: #slider-thumb dragTo #slider-track -> value 50 (midpoint) POST /api/drag-report. Reorder #sort-a dragTo #sort-c -> order changes, POST /api/drag-report. (act:28-29)
- [run 1 · 603027ms] Fake stream: GET /api/fake-stream has mime text/event-stream but is an ordinary finite body (XML envelope); page reads .text(). disco captures the body fine (it is NOT streaming). Contrast the real SSE which is body_state missing. (act:8)
- [run 1 · 603027ms] PERF gotcha: a backgrounded/occluded child window (or losing foreground) throttles rAF to ~1s, making clicks take ~3s. bringToFront / close the child restores ~60fps and ~100ms clicks. (act:45-51)
