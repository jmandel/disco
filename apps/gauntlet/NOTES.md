# gauntlet — notes

Appended by `disco note` / `s.note()`. Distill into README.md when it settles.

- [run 1 · 803440ms] recon: SPA, one HTML shell (/) + /app.js; JSON API under /api/*; standing WebSocket + standing EventSource /api/notify-sse from load; two iframes (same-origin /iframe.html which nests /iframe2.html, cross-origin http://localhost:4801/xframe.html). act:2
- [run 1 · 803443ms] GET /ctl returns the whole knob set; POST /ctl {..} merges and returns the effective set; POST /ctl/reset restores defaults. Defaults: slowMs 400, renderDelayMs 0, modal false, modalDelayMs 0, toastMs 2000, saveFails false, ambient false, heartbeatMs 5000, pollHoldMs 3000, wsPushMs 7000, timeoutMs 0, rerenderOnHover true, requireAuth false, notifyPollHoldMs 25000, notify false, xOrigin http://localhost:4801.
- [run 1 · 803443ms] s1 chart: #load-chart fires 3 concurrent GETs — /api/slow?ms=<slowMs>, /api/chart/a, /api/chart/b. #chart-status goes "loading…" then BACK to "idle", so it is useless as a postcondition (already-true both sides). The real anchor is #chart:has-text("Chart loaded"). act:3, act:13, act:123
- [run 1 · 803443ms] s1 chart: with ctl.renderDelayMs=900 (read at page load) the DOM lags the wire by ~900ms — #chart was still "" when /api/slow landed and filled 1285ms later. renderDelayMs affects the chart only; records and search render immediately. act:139
- [run 1 · 803443ms] s2 records: #record-N -> GET /api/record/N -> {id,name,dob,mrn,allergies[]}. Anchor: #record h3:text-is("Record N"). act:4, act:5
- [run 1 · 803444ms] s2 modal: gated by ctl.modal only (NOT by the record's allergies) — with modal:true all five records raise div#record-modal [role=dialog] "Allergy Review Required"; #modal-ack closes it. act:56, act:58-66
- [run 1 · 803444ms] s2 modal delay: ctl.modalDelayMs makes the dialog land AFTER the record renders, so an any-of that includes the record arm returns which="record" and the overlay arrives later and occludes the NEXT click (diagnosis occluded, over div#record-modal.overlay). act:76, act:80
- [run 1 · 803444ms] s3 save: click #save -> POST /api/save 202 {"form":{"name":"x"}} then GET /api/save/status?id=N. #save-state flips to "Saved ✓" OPTIMISTICALLY and never corrects itself, even when the save fails. Truth is the status code of /api/save/status: 200 ok, 500 failed (ctl.saveFails). A [role=status] toast says "Saved" or "Save failed (async)" and disappears after ctl.toastMs. act:11, act:74
- [run 1 · 803444ms] s3 save: the page never reads the /api/save/status body, so the row is body_state pending/missing — the status code is all you get, which is enough. act:74
- [run 1 · 803444ms] s4 spinner: #spinner is a pure CSS animation (animationName "spin"), no request, never resolves. until {gone:"#spinner"} always burns the full budget. act:126
- [run 1 · 803444ms] s5/22 ambient: ctl.ambient=true (read at page load) starts GET /api/heartbeat every heartbeatMs, a reissuing long-poll GET /api/poll holding pollHoldMs, and a WS push every wsPushMs. Counters live in the header: #heartbeat-count, #poll-count, #ambient-status. These rows carry action_id=NULL between acts and appear in whatever act window they overlap.
- [run 1 · 803444ms] s6 websocket: opened on load; server sends {"type":"hello","id":N,"state":{...ctl}}. Every button click EXCEPT #noop sends out {"type":"action","id":"<element id>","t":<epoch ms>}. Header #ws-status/#ws-count/#ws-last. Verified: #noop adds no out frame, #alert does. act:114-116
- [run 1 · 803444ms] s7 search: #search is debounced 250ms trailing; s.type (keystrokes) works, and the postcondition is {request:"/api/search", landed:true} -> {"q":..,"hits":[..]}. Results render synchronously with the response. act:14
- [run 1 · 803444ms] s8 rows: #load-rows -> GET /api/rows (484.7K, fully captured). 10000 rows, 24px each, virtualised into ~23 .row divs inside #rows-inner (height 240000px). #rows-count is an EMPTY span before the click, so {selector:"#rows-count"} is already-true; use #rows-count:has-text("rows"). Read row N from the wire (store.latestJson("/api/rows")), not the DOM. act:88
- [run 1 · 803444ms] s9 rerender: #rerender is replaced on every mousemove (ctl.rerenderOnHover), so a real mouse click times out with diagnosis "detached". {js:true} works and #rerender-count increments. act:16 (detached), act:19 (js ok)
- [run 1 · 803444ms] s10 iframes: frame:"#same-origin" and the nested frame:"#same-origin >> #nested2" both work; POST /api/iframe-submit. Cross-origin frame:"#cross-origin" is http://localhost:4801/xframe.html and posts to POST http://localhost:4801/api/xframe-submit — requests from the other origin ARE in the log. act:48, act:50, act:144
- [run 1 · 803444ms] s11 dialogs: #alert -> alert "Hello from gauntlet"; #confirm -> confirm "Proceed?" (#confirm-result "confirmed" on accept, "cancelled" on dismiss); #arm-unload arms beforeunload (#unload-armed "armed"), then #nav-away raises a beforeunload dialog. With dialogs:"accept" the navigation to /away.html completes; with dialogs:"dismiss" the page stays. act:42-45, act:157-158
- [run 1 · 803444ms] s12 session timeout: ctl.timeoutMs (read at load) -> #timeout-state "armed (N ms)"; after N ms idle it becomes "expired" and div#session-timeout [role=dialog] "Session expiring" appears. #stay closes it and re-arms (fired again 1284ms later). act:118, act:132
- [run 1 · 803444ms] s13 noop: #noop does nothing at all (no request, no WS frame, no DOM change) — the bare-act baseline of ~700ms window. #noop-disabled is disabled AND pointer-events:none inside div.field-wrap, so elementFromPoint returns the parent; disco reports diagnosis "disabled" (the disabled check runs first). act:7, act:8
- [run 1 · 803444ms] s14 delete: #delete -> DELETE /api/item/1 -> {"deleted":1}; #delete-result "deleted 1". act:35
- [run 1 · 803445ms] s15 child window: #open-child opens http://localhost:4800/child.html as a real second page. Its traffic is logged (GET /api/child-ping -> "pong"). There is no until predicate for "a new page appeared" — act bare and read report.pages / s.context.pages(), then closeOtherPages(). act:51, act:145
- [run 1 · 803445ms] s16 canvas: #grid is 400x200 with a 4x8 grid of 50px cells drawn on it, seeded from GET /api/grid. Nothing in the DOM changes on click; the readable state is window.__gridSelected = {r,c} and the cell pixel turning 255,213,79. Clicking a specific cell needs raw coordinates (s.page.mouse.click), because act has no position option. act:55 (centre click), verified r1/c2
- [run 1 · 803445ms] s17 combobox: #med. type (not fill) -> GET /api/meds?q=<prefix> (subsequence match) -> #med-list [role=listbox] with li[role=option]. The options are pointer-events:none: a click gives diagnosis "unclickable". Recipe: type -> until {request:"/api/meds",landed:true} -> press ArrowDown k times (aria-selected moves) -> press Enter -> until #med-selected:has-text("<name>"). act:24, act:28 (unclickable), act:29-31
- [run 1 · 803445ms] s18 shadow DOM: #shadow-host has an OPEN shadow root; Playwright pierces it — s.click("#shadow-host >> #shadow-btn") works and #shadow-count increments. act:32
- [run 1 · 803445ms] s19 SSE: #start-sse -> GET /api/sse (text/event-stream). #sse-status idle -> open -> done; 5 events ~500ms apart land as <li> in #sse-log. The request row is body_state "streaming" while open and "missing" once it ends — SSE messages are NOT in the log, only in the DOM. act:148
- [run 1 · 803445ms] s20 graphql: #gql-query POSTs {"query":"query { patient { name } }"}, #gql-mutate POSTs {"query":"mutation { rename(name: \"Renamed\") { name } }"} — both to POST /api/graphql 200. The response echoes operation and sawMutation, so the operation is only distinguishable from the REQUEST body (req_body column), not the path. act:36, act:37
- [run 1 · 803445ms] s21 auth: ctl.requireAuth=true makes EVERY html page 302 to /login.html?next=<path> (including /). POST /api/login accepts ANY user/pass and sets cookie gauntlet_auth=<user>; Path=/; HttpOnly. The login page does not read the response body (body_state missing) — the 200 is the signal. /secure.html then renders "Welcome, <user>". context.clearCookies() is the logout. act:92, act:97, act:98, act:99
- [run 1 · 803445ms] s23 push: POST /ctl {"push":"ws"|"sse"|"poll"} delivers one notification; #notif-count increments and #notif-list gains "Result N via <channel>". ws and sse arrive in <5ms on the standing channels. The poll channel only exists when ctl.notify=true — GET /api/notify-poll holds notifyPollHoldMs and reissues; the delivered body IS captured ({"n":4,"via":"poll","text":"Result 4 via poll"}). Without notify:true a push:"poll" is silently dropped (30s wait, nothing). act:107-112
- [run 1 · 803445ms] s24 context menu: rightclick #ctx-target reveals ul#ctx-menu [role=menu] with #ctx-open/#ctx-rename/#ctx-delete; clicking one sets #ctx-result "ctx: Rename" and hides the menu. act:20, act:21
- [run 1 · 803445ms] s25 dblclick edit: dblclick #dbl-target swaps in input#dbl-input and #dbl-state "editing"; fill then press Enter commits — #dbl-state becomes "committed: <value>" (not "saved") and #dbl-target shows the new text. act:22, act:151-153
- [run 1 · 803445ms] s26 drag: s.drag(a,b) only takes a SELECTOR as the destination, so it cannot address a point on #slider-track. Use s.page.mouse move/down/move(steps)/up: dragging #slider-thumb to 75% of the track gives #slider-value 77. Reordering #sort-list with dragTo landed a,b,c -> b,a,c (one slot); a stepped mouse path past the bottom of #sort-c gives b,c,a. Each drop POSTs /api/drag-report {"widget":"sort","order":"b,c,a"}. act:33 (error), act:34, probe9
- [run 1 · 803445ms] s28 fake stream: #load-fake-stream -> GET /api/fake-stream 200 with mime text/event-stream but an ORDINARY finite body (an XML envelope, 97 chars) that the page reads with .text(). disco captures it normally (body_state ok) — the event-stream mime does not mean streaming. act:38
- [run 6 · 64623ms] store hazard: apps/<app>/store outlives browsers and t/t_start restart per run, so latestJson() and ORDER BY t DESC can return a row from a previous run. Scope wire reads to rep.action (jsonFrom/statusFrom in lib.ts); for ws_frames use seq (global AUTOINCREMENT), never t. Caught by a cold run-check: section 6 read section 28's out-frame from the previous run.
- [run 6 · 64643ms] check.ts: 30/30 green on a cold browser and on a warm one (~29 s). Sequence: reset ctl + clearCookies + reload, then one step per section, then reset. Every knob flipped mid-check is put back inside its own step.
