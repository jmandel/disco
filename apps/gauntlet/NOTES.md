# gauntlet — notes

Appended by `disco note` / `s.note()`. Distill into README.md when it settles.

- [run 1 · 677001ms] recon: GET / is one static HTML shell with 27 numbered <section>s; /app.js (20.5K) is the whole client. No SPA router, no framework. All ids are stable and literal (act:1).
- [run 1 · 677019ms] GET /ctl returns the effective knob set; POST /ctl patches it and returns the new set; POST /ctl/reset restores defaults. Every behaviour that is 'sometimes' is a knob, not randomness.
- [run 1 · 677019ms] act:2 #load-chart fans out 3 concurrent GETs: /api/slow?ms=<ctl.slowMs>, /api/chart/a, /api/chart/b. #chart-status NEVER leaves 'idle' - the spinner lies. Postcondition is #chart:has-text('Chart loaded').
- [run 1 · 677019ms] act:86 with ctl.slowMs=1200 the same click takes 1787ms and still ends on 'Chart loaded (3 responses)' - the count is responses, not series.
- [run 1 · 677019ms] act:7 #record-1 -> GET /api/record/1 -> <h3>Record 1</h3> + <li> per field. Record fields also arrive on the wire, so read them from the log, not the DOM.
- [run 1 · 677019ms] act:8 with ctl.modal=true the same click also opens div#record-modal 'Allergy Review Required'. act:9 clicking another record while it is open is diagnosed occluded, naming div#record-modal.overlay in 171ms.
- [run 1 · 677019ms] act:88-92 all five records carry allergies, so with modal:true every record raises the dialog; with modal:false none do. The gate is the knob, not the record.
- [run 1 · 677019ms] act:93 ctl.modalDelayMs=900 pushes the dialog 900ms past the record render - a bare 700ms window misses it entirely. Always wait on #record-modal, never on the window.
- [run 1 · 677019ms] act:3 #save is optimistic: POST /api/save 202 then GET /api/save/status?id=N. UI flips to 'Saved OK' immediately and NEVER flips back.
- [run 1 · 677019ms] act:49 with ctl.saveFails=true the status request is 500 and the only UI signal is a transient [role=status] toast 'Save failed (async)' that vanishes after ctl.toastMs (2000). The durable truth is the status code on the wire.
- [run 1 · 677019ms] /api/save/status body is never read by the page: body_state goes pending -> missing. Status code is all you get - exactly the README's 'body not read by the page' case.
- [run 1 · 677019ms] act:51 #spinner never goes away. until {gone:'#spinner'} costs precisely the budget you gave it and returns a diagnosis + shot. Do not wait on it, ever.
- [run 1 · 677019ms] act:11 #search is debounced 250ms trailing: s.type('#search','ada') produced ONE request GET /api/search?q=ada.
- [run 1 · 677020ms] act:14 #med is NOT debounced: typing 'asp' produced three requests (q=a, q=as, q=asp). Same-looking widgets, different wire behaviour.
- [run 1 · 677020ms] act:72-74 combobox recipe: type -> until {request:'/api/meds?q=<full>', landed:true} -> press ArrowDown (sets aria-activedescendant=med-opt-0) -> press Enter -> until #med-selected. Options live in ul#med-list[role=listbox] and disappear on selection.
- [run 1 · 677020ms] act:76 GOTCHA: s.type() APPENDS. Typing 'asp' into a #med that already held 'Aspirin' produced /api/meds?q=Aspirinasp and a 5s timeout. Clear with s.fill(target,'') first.
- [run 1 · 677020ms] act:12/83 #load-rows fetches /api/rows once (484.7K, 10000 rows) and virtualises: only 23-28 .row nodes exist. Row 5000 is on the wire, not in the DOM.
- [run 1 · 677020ms] act:85 to move the window: s.evaluate('#rows.scrollTop = 24*5000') then until {fn} on the first row's dataset.id changing. Row height is 24px, inner height 240000px.
- [run 1 · 677020ms] act:13 #rerender is replaced on hover (ctl.rerenderOnHover), so a real mouse click is diagnosed detached after the full 3s actionability budget. act:16 {js:true} does it in 33ms.
- [run 1 · 677020ms] act:24/25 iframes: {frame:'#same-origin'} and {frame:'#cross-origin'}. A not-found inside a frame lists the FRAME's controls, which is how I learned the ids (#if-name/#if-submit, #xf-name/#xf-submit).
- [run 1 · 677020ms] iframe depth 2: s.frame('#same-origin >> #nested2') reaches #deep-name/#deep-submit in /iframe2.html. The aria diff never shows any of it - iframes appear as a node only.
- [run 1 · 677020ms] act:29 the cross-origin frame is served from http://localhost:4801 (ctl.xOrigin) and POSTs /api/xframe-submit to its own origin; the row is in the same log with host=localhost:4801.
- [run 1 · 677020ms] act:37/38/40 native dialogs are handled by the session policy (default accept) and recorded in `dialogs` with type+message: alert 'Hello from gauntlet', confirm 'Proceed?', beforeunload ''.
- [run 1 · 677020ms] act:39 #arm-unload flips the page to armed; act:40 the /away.html link then raises beforeunload, which the default policy accepts, so you land on /away.html. Return with navigate('/') + until #load-chart.
- [run 1 · 677020ms] act:31/48 #open-child opens a popup at /child.html. until {page:'child.html'} catches it in ~145ms; the report header then says '(+1 other page open)'. Call s.closeOtherPages() or the background page is throttled.
- [run 1 · 677020ms] act:30/36 canvas: 400x200 with 4 rows x 8 cols = 50px cells. A click at position {x,y} paints that cell #ffd54f. No DOM, no wire, no ws - the only assertion is getImageData via s.evaluate (or a shot).
- [run 1 · 677020ms] act:18/19 right-click on #ctx-target reveals ul#ctx-menu[role=menu]; #ctx-open/#ctx-rename/#ctx-delete set #ctx-result to 'ctx: <Item>' and close the menu.
- [run 1 · 677020ms] act:20 dblclick #dbl-target swaps the div for an <input> and #dbl-state goes idle -> editing.
- [run 1 · 677020ms] act:69 slider: s.drag('#slider-thumb', {dx:120}) -> value 43 (track 302px wide, thumb 22px, value = percent of travel). Each release POSTs /api/drag-report {widget:'slider',value}.
- [run 1 · 677020ms] act:70 GOTCHA: s.drag('#sort-a','#sort-b') does NOT reorder - one straight move to the ADJACENT item's centre never crosses its midpoint. It still POSTs /api/drag-report with the unchanged order, which looks like success on the wire.
- [run 1 · 677020ms] act:80 s.drag('#sort-a','#sort-c') DOES reorder, by one slot: a,b,c -> b,a,c. A manual s.page.mouse path with steps+overshoot moves it to the end: b,c,a.
- [run 1 · 677020ms] act:21/22 GraphQL is a plain POST /api/graphql with {query}. The response echoes operation:'query'|'mutation' and sawMutation - so the server does parse it. Both are 200; failure would not be a 4xx.
- [run 1 · 677020ms] act:23 /api/fake-stream is served as mime text/event-stream but is a finite XML body. body_state is 'ok' and the 97 bytes ARE captured - only genuinely endless event-streams show body_state 'streaming'.
- [run 1 · 677020ms] /api/notify-sse is the genuinely endless one: status 200, body_state 'streaming', t_end null, messages never captured. SSE-delivered content is observable ONLY in the DOM.
- [run 1 · 677020ms] act:59-61 push channels: POST /ctl {push:'ws'|'sse'|'poll'} delivers 'Result N via <channel>' into ul#notif-list. ws is visible as a ws_frame, poll as a /api/notify-poll response, sse only in the DOM.
- [run 1 · 677020ms] act:63 the long-poll channel only exists when ctl.notify=true: the page then holds GET /api/notify-poll (pending, up to ctl.notifyPollHoldMs=25000) and reissues it the instant it lands.
- [run 1 · 677021ms] BIG GOTCHA: ws_frames are captured only for sockets opened while YOUR session was attached. The socket opens on page load, so a session that joins an already-loaded page sees zero frames and until {ws} silently burns its budget. Navigate (reload) inside the session first.
- [run 1 · 677021ms] act:65 ctl.ambient=true turns on GET /api/heartbeat every 5000ms and a reissuing GET /api/poll that holds 3000ms. Both land inside whatever act happens to be open - they are the 'poll in every report'.
- [run 1 · 677021ms] act:67 ctl.timeoutMs=2500 raises a [role=dialog] 'Session expiring' after that much idle and sets #timeout-state to 'expired'. act:68 'Stay signed in' dismisses it and resets the state to 'off'.
- [run 1 · 677021ms] act:52 ctl.requireAuth=true makes GET /secure.html 302 to /login.html?next=/secure.html. Credentials are admin/admin; POST /api/login sets cookie gauntlet_auth=admin (HttpOnly), so document.cookie is empty - read it from resp_headers or context.cookies().
- [run 1 · 677021ms] act:53 with the cookie, /secure.html is 200 and #who reads 'Welcome, admin'. Clear with s.context.clearCookies() to test the refusal path.
- [run 1 · 677021ms] act:95 shadow DOM: the host has an OPEN shadowRoot, so an ordinary CSS descendant selector '#shadow-host #shadow-btn' pierces it. The count lives inside the root and is unreachable from the aria diff - assert with until {fn} through .shadowRoot.
- [run 1 · 677021ms] act:5/34 #noop-disabled is diagnosed 'disabled' in ~100ms, before any hit test, so the pointer-events:none trap on its .field-wrap parent never fires. #noop is the true no-op: it costs exactly the 700ms window and produces nothing.
- [run 1 · 677021ms] act:6 DELETE /api/item/1 is the only non-POST write; response {"deleted":1} and #delete-result shows 'deleted 1'.
- [run 1 · 677021ms] the header statusbar (ws/frames/ambient/heartbeats/polls/last ws frame/effective ctl) changes on nearly every act. Set s.uiIgnore = [/ws: open/, /last ws frame/, /effective ctl/, /heartbeats/] or every report is noise.
- [run 2 · 40984ms] CORRECTION to the auth note: POST /api/login accepts ANY non-empty user/pass and sets gauntlet_auth=<user>. Only an empty field is 401 'user and pass required'. admin/admin is not a credential, it is just the first thing I tried.
- [run 2 · 40990ms] ctl.requireAuth guards the SHELL too, not just /secure.html: GET / becomes 302 -> /login.html?next=/. Every home anchor needs #login as an arm or a refusal costs a 5s timeout instead of 40ms.
- [run 2 · 40990ms] /away.html's return link is a#back (not a#home; a#home is the one on /secure.html). Two pages, two ids, same words 'Back to the gauntlet' - anchor on the id, not the text.
- [run 2 · 40990ms] login() must reload the login form between attempts: a leftover 'login failed' in #login-error makes the {selector:'#login-error:not(:empty)'} arm alreadyTrue and reached() rejects it (correctly).
- [run 2 · 40990ms] slider drag: assert with until {all:[value-changed, {request:'/api/drag-report'}]} - the until returns the moment the DOM value changes, which can be BEFORE the mouseup POST is issued, so an action-scoped store read finds nothing.
- [run 2 · 40990ms] run-check: 33/33 on a warm browser and 33/33 after ./disco close gauntlet, ~25.7s.
