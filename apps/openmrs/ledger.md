# Variability ledger — OpenMRS O3 @ dev3.openmrs.org (store run 2, 2026-08-30)

Observed vs inferred, with n-counts, and the probe that would settle each open question (GUIDANCE §7.5).
Evidence = act ids / request ids in `apps/openmrs/store/store.sqlite` (run 2) and the two `run-check` scratch
stores (`.scratch/checks/openmrs-*`, one kept). *prior* = expectation from the enterprise-SPA prior (§8), not
observation. Rows marked ✔ are handled by `lib.ts` in both directions.

| # | step | observed (n) | varied / surprise | hypothesis | resolving probe |
|---|---|---|---|---|---|
| 1 | reaching the login page | 3 loads (run 2 initial, act:19 logout, run-check act:1 ×2) | a fresh profile renders the form before React handlers attach: run-check #1 lost the username fill (Continue swallowed by `required`, zero DOM change); run #2 did not | hydration timing depends on cold cache (~50 JS chunks) | ✔ `login` verifies the fill (`#username.value`) and retries Continue once. Probe: `DISCO_KEEP_SCRATCH=1 bun scripts/run-check.ts openmrs` ×5 on a cold profile and count retries (`actions` where `sel='#username'`) |
| 2 | location picker after Log in | **0/3** logins showed it (act:4, act:31, run-check act:5); reached only via header "Change location" (act:5–8, n=1) | absent because `userProperties.defaultLocation` is set for admin | the picker appears when the user has no default location or a deployment forces it (`?update=true` is the change-location variant) | ✔ handled as optional (radio by label + Confirm). Probe: log in as a user without `defaultLocation`, or clear it with `POST /user/<uuid> {userProperties:{defaultLocation:null}}` (a write) and log in again; expect `/spa/login/location` without `update=true` |
| 3 | login failure shape | n=2 (act:23, act:27): HTTP **200** `{authenticated:false}`, inline `role=status` error, form back to the username step | no 401, and the daemon tagged the request `ambient` (see friction #4) | O3's login app uses the session GET and reads `authenticated` | ✔ `login` throws with the notification text. Probe: a locked-out user (loginAttempts) — does the message differ? |
| 4 | first-run interstitials | n=1 fresh headless profile + 2 fresh run-check profiles: **no** onboarding tour, no "what's new", no cookie banner | `div.react-joyride` is mounted (the `user-onboarding-app` loads) but never showed a step | the tour is gated by a user property / config we did not trigger, or by viewport | Probe: `disco sql openmrs "SELECT … FROM bodies_fts WHERE bodies_fts MATCH 'joyride'"` on the config bodies; log in as a brand-new user; try a 1280×800 viewport |
| 5 | ambient traffic on home | `/queue-entry` ×2, `/fhir2/R4/Location` ×2, `/obs?patient=…` ×3 every ~60s; one 120s gap (t 107s → 227s); never auto-classified (cv 1.33–1.86 from 0-gap bursts) | families are shared with real, action-caused fetches (`Location?_tag=Login+Location` is the picker's list; `queue-entry` is the home page's own load) | SWR `refreshInterval` per widget; headless tab throttling explains the skipped cycle | manual `disco families --ambient` for `queue-entry` + `obs` only. Probe: idle 5 min with a visible (non-headless) window and re-read `families.evidence`; check whether the chart pages poll at all (none seen in ~10 min on the chart) |
| 6 | search results | "Susan" → 4 hits, totalCount 4 (act:10, act:13/14, run-check); "Susan Lopez" → 1 hit (act:16/17) | `limit=10` — the overlay shows 10 max; the full page paginates | `totalCount` is the truth for "how many" | Probe: a one-letter query (`q=a`) — does the page request `startIndex` pages when scrolling? |
| 7 | allergies body | n=1 patient (Susan Lopez): empty Bundle, "There are no allergy intolerances to display" | the populated shape (`reaction[].manifestation`, `criticality`) is **inferred from FHIR R4**, not observed | — | Probe: open a patient with allergies (search the queue list; or FTS the store for `"AllergyIntolerance"` bodies with `total>0` after visiting a few charts) and confirm `extractSummary().allergies` fields |
| 8 | session expiry / keepalive | not observed; longest idle 2.5 min; the app re-GETs `/session` on every route change | — | *prior*: Tomcat session timeout (30 min default) with no warning modal; the next act would land on `/spa/login` | Probe: idle 35 min, then `act` anything; expect `whereAmI() === "login.username"` (`login()` recovers). Watch the `session_expiry` sentinel (fired once spuriously in run 2, ev ≈ 3087, on the logout page) |
| 9 | chart open settlement | n=6 (act:11, 15, 18, run-check ×3): `still-active` 1.8–2.7s or `settled:visual` 42ms with `until` 0.8–2.0s; 100–136 requests | which verdict you get depends on whether the JS chunks were cached | the banner (`Patient/<uuid>` FHIR) lands well before the widgets | ✔ anchor on the banner + the specific FHIR body. Probe: `diffTrace(act:11, act:18)` for the request-set difference (cache) |
| 10 | vitals set completeness | Susan Lopez: latest set has 8 values (temp, sys, dia, pulse, SpO2, resp, weight, height); 64 vitals + 16 biometric observations | BMI on screen is not on the wire (computed); `165095` code set returned 0 entries | some patients will lack whole vitals sets | `extractSummary` groups by the latest `effectiveDateTime` — a partial set is returned as-is. Probe: sample 5 queue patients and diff `Object.keys(vitals.values)` |
| 11 | writes during read flows | chart open → `POST /user/<uuid>` (n=6/6); location Confirm → `POST /session` + `POST /user/<uuid>` (n=1); logout → `DELETE /session` (n=1) | a read-only stance cannot open a chart without a userProperties write | app-side, unconditional | none; declared in `lib.ts`/`check.ts` headers |
| 12 | `GET /module → 500` after logout | n=1 (act:19) | server error on the login page, no visible effect | the module list needs auth; the login page asks anyway | ignore; watch the error sentinel |
| 13 | dev3 data churn | Susan Lopez (`e0167f26-…`) existed for the whole session; dev3 is a shared dev server | names/uuids may be reset or edited by other users | *prior* | `check.ts` picks a name off the live `queue-entry` body first and falls back to "Susan" |
| 14 | o3.openmrs.org | run 1: Cloudflare Turnstile 403 in headless Chromium; curl with a browser UA gets 200 | headless is bot-scored | — | Probe: `--launch` without `--headless` (or `--attach` to a human-driven Chromium) and see whether Turnstile auto-passes; the pack is host-parametrised by `ORIGIN` in `lib.ts` |
| 15 | post-login toast "Some modules have unresolved backend dependencies" | 1/4 logins (act:31 only; not act:4, act:31's predecessor act:27 failed, not run-check ×3) | appeared after a logout → login cycle in the same tab | `implementer-tools` checks backend modules once per app boot and warns when a frontend module's backend is missing on dev3; timing of the check vs. the login decides whether it lands on the login page or the shell | ✔ `login` dismisses it if present (`actIfPresent`, act:32). Probe: 5 logout/login cycles; note whether it also appears on the login page itself (where nothing dismisses it) |

## Standing open questions for the user
1. Is the stance for future sessions read-only? If so, note that **every chart open writes** `userProperties.patientsVisited` (row 11) — unavoidable from the UI.
2. Are writes to demo data acceptable for probes 2 (clear `defaultLocation`) and 7 (record an allergy on a demo patient)?
3. Should the pack target o3 (production-like, Cloudflare) or dev3 (pre-release builds, churny data)? `ORIGIN` is one line.
